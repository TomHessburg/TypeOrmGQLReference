/**
 * Just placeholder stuff for now
 * Really should be mocking the DB some how.
 * Really should just test resolers manually and use DI to mock
 * OR just host a PG DB seprately somewhere specifically for testing
 * reference: https://www.youtube.com/watch?v=23w8PSHwep0
 */

// import { request } from "graphql-request";
// import { hostUrl } from "./constants";

// const EMAIL = "asoijadoij@aosjdaosijda.com";
// const PASSWORD = "123asd23";

// const mutation = `
//     mutation {
//         register(email: "${EMAIL}", password: "${PASSWORD}")
//     }
// `;

// test("register user", async () => {
//   const response = await request(hostUrl, mutation);
//   expect(response).toEqual({ register: true });
// });
